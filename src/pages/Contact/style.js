import { Box, styled } from "@mui/material";

export const boxProfile = styled(Box)({
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    width: "100%",
    minHeight: "100vh",
    backgroundColor: "#FFF",
});
  